class SplashController < ApplicationController

	def index
		# if user_signed_in?
		# 	if session[:g]
		# 		redirect to '/groups/#{session[:g]}'
		# 	else
		# 		redirect_to "/dashboard"
		# 	end
		# end
	end
	
end
